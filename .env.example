# 容器编排管理平台 - 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# ===========================================
# 数据库配置
# ===========================================
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=container_management
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# ===========================================
# Redis配置
# ===========================================
REDIS_HOST=redis
REDIS_PORT=6379

# ===========================================
# JWT配置
# ===========================================
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRE_HOURS=24

# ===========================================
# 日志配置
# ===========================================
LOG_LEVEL=info
LOG_FORMAT=json

# ===========================================
# Kubernetes配置
# ===========================================
KUBECONFIG_PATH=

# ===========================================
# CORS跨域配置
# ===========================================
# 开发环境示例：
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://frontend:3000

# 生产环境示例（替换为你的实际服务器IP）：
# CORS_ALLOWED_ORIGINS=http://192.168.1.100:3000,http://your-domain.com:3000

# 多域名配置示例（逗号分隔）：
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.100:3000,http://your-domain.com,https://your-domain.com

# 当前配置（默认允许本地访问）：
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://frontend:3000

# ===========================================
# 前端配置
# ===========================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# ===========================================
# 服务器配置（根据实际情况修改）
# ===========================================
# 如果你部署在特定IP地址上，请修改下面的配置
# 例如：CORS_ALLOWED_ORIGINS=http://你的服务器IP:3000,http://localhost:3000